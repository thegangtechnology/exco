def add_number(x: int, y: int) -> int:
    """Add two argument together
    Args:
        x (int): first argument
        y (int): second argument
    Return:
        int. The sum of x and y
    """
    return x + y

# def subtract_number(x: int, y: int) -> int:
#     """Find the difference between x and y
#     Args:
#         x (int): first argument
#         y (int): second argument
#     Return:
#         int. The difference of x and y
#     """
#     return x - y
